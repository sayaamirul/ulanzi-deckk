appId: com.ulanzi.deckk
productName: Ulanzi DecKK
directories:
  output: dist
files:
  - out/**
  - package.json
extraResources:
  - from: udev/99-ulanzi-d200h.rules
    to: udev/99-ulanzi-d200h.rules
linux:
  target:
    - AppImage
  category: AudioVideo
  artifactName: ${name}-${version}-${arch}.${ext}
